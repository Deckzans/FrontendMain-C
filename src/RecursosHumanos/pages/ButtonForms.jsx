import { Box, Button } from "@mui/material"

export const ButtonForms = ({ text }) => {
    return (
        <Box>
            <Button>
                {text}
            </Button>
        </Box>
    );
}
